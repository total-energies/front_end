import styles from "../styles/AppName.module.css";

export default function AppName() {
    return (
        <div className={styles.divposition}>
          <h4 className={styles.heading}>Q & A ToolBox</h4>
        </div>
    );
}